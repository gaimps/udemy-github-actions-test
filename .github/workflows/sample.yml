name: Shell Command

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eccho "hello world"
      - name: run multiple command
        run: |
          node -v
          npm -v
